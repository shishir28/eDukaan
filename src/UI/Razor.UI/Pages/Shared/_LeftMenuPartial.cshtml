<div class="row">
    <div class="col-sm-3">
        <div class="left-sidebar">
            <h2>Category</h2>
            <div class="panel-group category-products" id="accordian">
                <!--category-products-->
                <form asp-page-handler="filterCatalogByCategory" method="post">
                    <input type="hidden" name="categoryCode" id="hiddenCategoryCode" />
                    @foreach (var productCategory in Model.ProductCategoryList)
                    {
                        <div class="panel panel-default">
                            <div class="panel-heading">
                                <h4 class="panel-title">
                                    <a href="#"
                                    onclick="categoryCodeClick('@productCategory.Code', parentNode.parentNode.parentNode.parentNode);">@productCategory.Name</a>
                                </h4>
                            </div>
                        </div>
                    }
                </form>
            </div>
            <!--/category-products-->

            <div class="brands_products">
                <!--brands_products-->
                <h2>Brands</h2>
                <div class="brands-name">
                    <form asp-page-handler="filterCatalogByBrand" method="post">
                        <input type="hidden" name="brandCode" id="hiddenBrandCode" />
                        <ul class="nav nav-pills nav-stacked brand-list">
                            @foreach (var productBrand in Model.ProductBrandList)
                            {
                                <li>
                                    <a href="javascript:;"
                                    onclick="brandCodeClick('@productBrand.Code', parentNode.parentNode.parentNode); ">
                                        <span class="pull-right">(@productBrand.ProductCount)</span>@productBrand.Name
                                    </a>
                                </li>
                            }
                        </ul>
                    </form>
                </div>
            </div>
            <!--/brands_products-->

            <div class="price-range">
                <!--price-range-->
                <h2>Price Range</h2>
                <div class="well text-center">
                    <input type="text" class="span2" value="" data-slider-min="0" data-slider-max="600"
                        data-slider-step="5" data-slider-value="[250,450]" id="inputFilterPrice"><br />
                    <b class="pull-left">$ 0</b> <b class="pull-right">$ 600</b>
                </div>
            </div>
            <!--/price-range-->

        </div>
    </div>

    <div class="col-sm-9 padding-right">
        <div classfeatures_items">
            <!--featur_items-->
            @foreach (var product in Model.PagedCatalog)
            {
                <div class="col-12 col-md-6 col-lg-4">
                    <partial name="_ProductItemPartial" model="@product" />
                </div>
            }
            <razorpager aria-label="Page navigation" tag-name="nav" total-item-count="Model.TotalItems"
                template-name="basic" page-size="Model.PageSize"></razorpager>
        </div>
    </div>

</div>
